import LoginImg from "../../img/login.svg"
import React from "react"

export const LoginIcon = ({className}:{className? : string})=>
<img className={className} src={LoginImg} draggable={false} alt="reset"/>
