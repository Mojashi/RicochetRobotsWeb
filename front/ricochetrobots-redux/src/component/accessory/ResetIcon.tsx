import React from "react"
import ResetImg from "../../img/reset.svg"

export const ResetIcon = ({className}:{className? : string})=>
    <img className={className} src={ResetImg} draggable={false} alt="reset"/>